# Planner notes — 2026-07-16

## Run #7 reconcile (disarm 01:24)
Both owner-eyeball defects shipped fixed: **v0.15.0 relit-energy** (DC-normalize env-SH to the
ambient slider — the ~4× bloom gone; |env−flat|=0.00014, render_matrix 10/10 deterministic ×2)
and **v0.16.0 normal-sign infra** (camera-hemisphere orient init+post-solve, sign-aware
smoothing, folded-coherence tripwire, density-invariant FAIL-CLOSED multi-scale gate — proven
fail-closed after 2 caught fail-opens). Suite 107. Gates green, scans clean, pushed + tags.

**D6 seeded OPEN by the factory (correctly):** camera-orientation resolves only the along-view
sign component — synthetic evidence shows 17–49% residual opposition at hero coherence with
grazing normals. The scheduled re-decompose is the objective arbiter: gate <5% ⇒ ships and
kills the patch shadows; gate fails ⇒ assets stay as-is (fail-closed, owner keeps current
look) and D6 picks a scalable grazing-normal resolver (visibility-weighted orientation in the
solve / anisotropy-gated flip / coarse voxel sign field — NOT the test-only MST).

Planner: placed the D4-runtime entry in decisions.md (factory-authored, planner-placed);
viewer relaunched for the relit-energy eyeball (V = shape/tint not energy jump).
Open owner words: (1) D6 re-decompose — which GPU box + go (hours; trader needs idle check);
(2) relit-energy eyeball verdict.

## Run #8 reconcile (flashlight-orb shipped)
**v0.17.0**: camera flashlight (point/spot) + engine-lit reference orb in the relight pass —
the Moon-Stone fireball prerequisite is now real: per-splat world position added to OUR
material buffer (32→48 B, object-space pos × instance matrix — the task's "already available"
premise was wrong, factory corrected it; NO PLY schema change, NO GDGS edit), light params in
a binding-5 UBO (push constant untouched), layout = the N=2–4 fireball extension point.
Verified by a 5-lens high-tier panel; the analytic gate was caught fail-open on the range term
by fault injection (same class as run #7's gate bugs) and re-proven fail-closed. Frame-time:
flashlight on/off within noise @ 2.4M splats — the fireball budget looks roomy. Suite 107,
render_matrix 10/10, gates green at reconcile, pushed + tag.
Run #7's chandoff lore (handoff #7) committed with this reconcile — it was untracked through
ce6005d. Remaining owner words: (1) F/O eyeball (acceptance gate), (2) D6 re-decompose
box + go (then pixel5 right after).
