# 2026-07-12 — Pre-arming review: fix workflow, seed factory tasks (planner)

## What happened
- Owner: "review this project, fix workflow problems, prepare tasks for dark factory,
  then say green" + mid-session mandate: *leave space in factory tasks for code quality
  reviews and keeping the project well structured*.
- Ran a 4-dimension multi-agent review (factory-readiness / config-vs-plugin /
  cross-doc / code-quality) + a D1 candidate survey. The verifier fleet + survey hit the
  session usage limit (resets 2am) — findings were hand-verified against source instead;
  survey resumed after the reset.
- **Everything found + fixed is in `docs/decisions.md` (2026-07-12 entry)** — highlights:
  assets/raw read-only-vs-ingest contradiction resolved (source data vs workspace),
  `"STATUS"`-case lane-guard config bug, ephemeral scratchpad pointers recovered into
  `prototype/` + `docs/img/`, root CLAUDE.md hardware/Commands corrections, task specs
  made executable-without-guessing, numeric acceptance gates.
- Discovered the dark-factory plugin is local-scoped to /home/lukas/Trader — **not active
  in splatworld**. Arming checklist (incl. this) is in the decisions entry.
- Queue now: READY = ingest-stage, **code-hardening (new, seeded from confirmed review
  findings)**, perf-budget; FILLER = **recurring-quality-pass (new, the owner's mandate)**;
  PARKED owner-gated = data-release; GATED = M2 (D1) … M5. New DECISIONS row **D2**
  (final per-asset Gaussian budget, fed by perf-budget's tradeoff table).

## Watch out
- Factory code lanes were respected: confirmed CODE defects (train_base asserts nothing,
  schema version unchecked on read, OPENCV silently accepted, …) were NOT fixed by the
  planner — they live in `tasks/2026-07-12-code-hardening.md`.
- ply_io.py:57 GDGS-centering note dangles (points at a decisions entry that never
  existed) — resolution is item 14 of code-hardening; matters for M4 placement.

## Post-run reconcile + next-run prep (planner, same day)
- First factory run shipped v0.2.0–v0.5.0 (4 tasks, see `docs/2026-07-12-handoff.md`).
  Reconcile: gates re-run green (29 tests, `SMOKE OK 23s`), diff scanned (no creds, no
  large binaries), **pushed** + tags — factory doesn't push by design (`allow_push:false`).
- Next-run prep: **M2 split** — M2a relight runtime (godot/ lane, works on placeholder
  attributes TODAY) + M2b decompose (precompute/ lane, phased A–D, license guardrails,
  `scaffold/` gitignored). Owner go-ahead given ("prep next run"). Fillers: docs-guide
  seeded; quality-pass first slice = broken `single_asset.tscn` (superseded — M2a rebuilds
  that scene). **D2 still awaits the owner's pick** (data-backed options in DECISIONS) —
  not a wall for M2a/M2b.

## New data: datasets/pixel5 (evening)
9 walkaround foliage clips, ALL 1440×1080@30 (owner expected some 4K — only the lone JPG is
4K/4080×3072; if 4K video was shot, originals didn't transfer). Walkaround coverage should
improve splat geometry vs pixel4's arcs. Seeded `tasks/2026-07-12-pixel5-variants.md`
(triage → ≥5 budgeted variants, feeds M4 + D2). Queue: run #3 = phase D → env-SH → video →
pixel5 variants.

## Run #3 reconcile (planner, 2026-07-13 early)
M2 COMPLETE + DEMONSTRATED: v0.8.0 phase D (real-asset budgets held), v0.9.0 env-SH ambient
(D4 done), v0.10.0 orbit demo video (README-embedded). Gates re-run green (45 tests, SMOKE OK),
range scanned, pushed + tags. Planner frame-eyeball of the video: relighting reads real
(raw=washed-out baked vs relit=saturated w/ moving specular); azimuth response subtle —
consistent with D5 normal isotropy. NOTE: D5 matters for M3 — the backlit term is dot(-N,L);
isotropic normals would mush the transmission glow. Clobber root-cause seeded to quality-pass.
Open for owner: D5 eyeball + D2 budget confirm (gates pixel5-variants).

## Owner viewer session feedback (2026-07-13)
Looks good overall; vision recorded: whole-scene coverage + distance-based splat decay (LOD)
"challenges hero million-poly models", promising for foliage BRUSHES (→ M4 row). Two defects:
(1) asset renders tilted (~120°/90°) — SfM gauge has no gravity; up-alignment belongs in
export (invariant) → `tasks/2026-07-13-ground-alignment.md` READY #1 (env-SH must rotate
identically). (2) per-splat SPARKLE in the orbit — owner: "bad for usable render" → D5
DECIDED (fix) → `tasks/2026-07-13-normal-quality.md` READY #2 (diagnose attribution first:
raw-vs-relit-vs-pruned). pixel5-variants re-gated on D2 + grounded export. D2 still the one
open owner word (500k + opacity-0.02 recommended).

## Metadata probe (2026-07-13, after owner's g-sensor point)
Owner corrected the tilt framing: not "SfM can't recover gravity" as an abstract — the phone
HAD it (g-sensor) and our data lost it. Probe: clips are from a **Pixel 7**; mp4 carries 3
`mett` tracks — track 3 (~276 KB, per-frame quaternion-like floats) ≈ the EIS gyro stream,
proprietary format (not CAMM) → RE = optional future upgrade, heuristic stands. ⚠️ Clips
embed **GPS location** in container tags → data-release row now mandates metadata stripping
before any public upload.
