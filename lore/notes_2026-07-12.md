# 2026-07-12 — Pre-arming review: fix workflow, seed factory tasks (planner)

## What happened
- Owner: "review this project, fix workflow problems, prepare tasks for dark factory,
  then say green" + mid-session mandate: *leave space in factory tasks for code quality
  reviews and keeping the project well structured*.
- Ran a 4-dimension multi-agent review (factory-readiness / config-vs-plugin /
  cross-doc / code-quality) + a D1 candidate survey. The verifier fleet + survey hit the
  session usage limit (resets 2am) — findings were hand-verified against source instead;
  survey resumed after the reset.
- **Everything found + fixed is in `docs/decisions.md` (2026-07-12 entry)** — highlights:
  assets/raw read-only-vs-ingest contradiction resolved (source data vs workspace),
  `"STATUS"`-case lane-guard config bug, ephemeral scratchpad pointers recovered into
  `prototype/` + `docs/img/`, root CLAUDE.md hardware/Commands corrections, task specs
  made executable-without-guessing, numeric acceptance gates.
- Discovered the dark-factory plugin is local-scoped to /home/lukas/Trader — **not active
  in splatworld**. Arming checklist (incl. this) is in the decisions entry.
- Queue now: READY = ingest-stage, **code-hardening (new, seeded from confirmed review
  findings)**, perf-budget; FILLER = **recurring-quality-pass (new, the owner's mandate)**;
  PARKED owner-gated = data-release; GATED = M2 (D1) … M5. New DECISIONS row **D2**
  (final per-asset Gaussian budget, fed by perf-budget's tradeoff table).

## Watch out
- Factory code lanes were respected: confirmed CODE defects (train_base asserts nothing,
  schema version unchecked on read, OPENCV silently accepted, …) were NOT fixed by the
  planner — they live in `tasks/2026-07-12-code-hardening.md`.
- ply_io.py:57 GDGS-centering note dangles (points at a decisions entry that never
  existed) — resolution is item 14 of code-hardening; matters for M4 placement.
