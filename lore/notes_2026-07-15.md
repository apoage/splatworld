# Planner notes — 2026-07-15

## Sidequest: Godot gaussian-splatting landscape survey (owner ask)
Workflow sweep (4 angles; verify pass lost to the session limit — facts from GitHub API,
unadversarially-verified). Full list delivered in-session; load-bearing findings:
- **GDGS is the de-facto standard** (only Asset Library entry, 241★, active) — good vendor bet.
- **No core-engine support anywhere**: zero godotengine proposals/PRs (all "splat" hits are
  terrain splatmaps); no W4 involvement.
- **A race exists but is young**: `LonKeyDotae/godot-gaussian-splatting-relight` (created
  2026-07-02, 0★) forks GDGS **at our exact pinned commit be61f8f** and injects relighting
  into `gsplat_projection.glsl` (+540 lines, importer-driven data). No precompute/delighting
  pipeline visible. Worth reading their shader diff. Also `4321ba/GodotGaussianSplattingNodes`
  (thesis WIP, active) lists "relightable .ply handling" on its TODO — three parties converging.
- Owner read: we're in good shape — relighting as a base feature + splats-as-assets is the moat.

## Demo north star (owner, "keep in mind")
Owner endorsed the "Moon Stone Meadow" concept (doc kept in HOME, outside this repo):
playable ambient meadow, day → touch moon stone → dusk lerp → fireballs; backlit foliage =
the transmission money shot; both phases on the relightable shader (honesty constraint).
Maps to M3 (backlit glow) + M4 (meadow carpet); NEW implied runtime work to attach to M4/M5
rows at next reconcile: point-light support in the relight pass (currently one directional),
touch trigger + day/night state, fireball light budget. The lighting-lab day-cycle is the
dusk-lerp rehearsal.

## Factory run #5 armed
Owner armed the factory (normal-quality step 2 → lighting-stability → pixel5-variants).
Planner in doc/lore/memory lanes only; these notes commit at reconcile.

## Run #5 reconcile (disarm 02:10)
Shipped **v0.13.0** (D5 step 2: k-NN normal smoothing in decompose, opt-in, PSNR −0.11 dB /
shimmer −75% / coherence 0.922 on the real re-decompose; suite 88). lighting-stability = WIP
(harness committed; 6/10 checks PASS — engine core stable: azimuth-360 59 dB, smoothness,
ambient floor; 4 FAILs are harness bugs with fixes listed in the WIP header). Gates re-run
green, scans clean, pushed + tag v0.13.0.

**Planner executed the slice-5 core** (highest user-visible value, owner asleep): promoted the
validated smoothed decompose → assets/built (originals kept as `*_unsmoothed.*.bak`),
re-exported (ring_normal_dot_up=1.0, 2.4M splats, no NaN), re-mirrored to gs_assets. The
viewer now loads the smoothed asset — owner sparkle re-eyeball pending. Remaining slice 5 =
factory: pxl_131945 re-decompose + demo/gif regen. Doc drift found: export docstring's
from-decompose example omits required `--in`.

Moon-Stone demo work items attached to M4/M5 gated rows per the memory note.
