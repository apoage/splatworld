# Planner notes — 2026-07-15

## Sidequest: Godot gaussian-splatting landscape survey (owner ask)
Workflow sweep (4 angles; verify pass lost to the session limit — facts from GitHub API,
unadversarially-verified). Full list delivered in-session; load-bearing findings:
- **GDGS is the de-facto standard** (only Asset Library entry, 241★, active) — good vendor bet.
- **No core-engine support anywhere**: zero godotengine proposals/PRs (all "splat" hits are
  terrain splatmaps); no W4 involvement.
- **A race exists but is young**: `LonKeyDotae/godot-gaussian-splatting-relight` (created
  2026-07-02, 0★) forks GDGS **at our exact pinned commit be61f8f** and injects relighting
  into `gsplat_projection.glsl` (+540 lines, importer-driven data). No precompute/delighting
  pipeline visible. Worth reading their shader diff. Also `4321ba/GodotGaussianSplattingNodes`
  (thesis WIP, active) lists "relightable .ply handling" on its TODO — three parties converging.
- Owner read: we're in good shape — relighting as a base feature + splats-as-assets is the moat.

## Demo north star (owner, "keep in mind")
Owner endorsed the "Moon Stone Meadow" concept (doc kept in HOME, outside this repo):
playable ambient meadow, day → touch moon stone → dusk lerp → fireballs; backlit foliage =
the transmission money shot; both phases on the relightable shader (honesty constraint).
Maps to M3 (backlit glow) + M4 (meadow carpet); NEW implied runtime work to attach to M4/M5
rows at next reconcile: point-light support in the relight pass (currently one directional),
touch trigger + day/night state, fireball light budget. The lighting-lab day-cycle is the
dusk-lerp rehearsal.

## Factory run #5 armed
Owner armed the factory (normal-quality step 2 → lighting-stability → pixel5-variants).
Planner in doc/lore/memory lanes only; these notes commit at reconcile.

## Run #5 reconcile (disarm 02:10)
Shipped **v0.13.0** (D5 step 2: k-NN normal smoothing in decompose, opt-in, PSNR −0.11 dB /
shimmer −75% / coherence 0.922 on the real re-decompose; suite 88). lighting-stability = WIP
(harness committed; 6/10 checks PASS — engine core stable: azimuth-360 59 dB, smoothness,
ambient floor; 4 FAILs are harness bugs with fixes listed in the WIP header). Gates re-run
green, scans clean, pushed + tag v0.13.0.

**Planner executed the slice-5 core** (highest user-visible value, owner asleep): promoted the
validated smoothed decompose → assets/built (originals kept as `*_unsmoothed.*.bak`),
re-exported (ring_normal_dot_up=1.0, 2.4M splats, no NaN), re-mirrored to gs_assets. The
viewer now loads the smoothed asset — owner sparkle re-eyeball pending. Remaining slice 5 =
factory: pxl_131945 re-decompose + demo/gif regen. Doc drift found: export docstring's
from-decompose example omits required `--in`.

Moon-Stone demo work items attached to M4/M5 gated rows per the memory note.

## Evening — owner eyeball on the smoothed asset: D5 CONFIRMED FIXED
Owner verdict (viewer, smoothed pxl_144634): "wow just wow… the moving sun effect… absolute
epic and subtle… like raytraced… no sparkles so far, whole thing is nice and stable." D5
closed end-to-end (metric −75% AND owner eyes). New asks seeded while run #6 armed:
- `tasks/2026-07-15-flashlight-orb.md` (Ready #4) — flashlight = point/spot in the relight
  pass (Moon-Stone fireball prerequisite pulled forward) + engine-lit reference orb toggle.
- quality-pass slice 6 — aesthetic splat cleanup via existing export prune flags, eyeball-gated
  ("just aesthetic" per owner, LOW).
Run #6 armed 21:08 (lighting-stability fixes → pixel5-variants → slice-5 tail).

## Run #6 reconcile (disarm 22:31)
Shipped **v0.14.0 lighting-stability** (10/10 repeatable gate, 53 conditions; correctness judge
caught falsely-lowered PSNR floors, fixed with real margin; fault-injection proved the raw-leak
check fires) + **slice-5 rollout to pxl_131945** (coherence 0.925, but rode the gate: −0.50 dB
cost, 0.48 dB headroom — owner eyeball advised, reversible). Gates green (88 + SMOKE OK),
scans clean, pushed + tag v0.14.0.

Planner post-run (lanes open): **FPS + frame-time + splat-count HUD line** (owner request;
frame ms from fps, not TIME_PROCESS — render thread must count) and **RELIGHT_ASSET env
override** in the controller; viewer launched on the smoothed pxl_131945 (2,075,806 splats
clean load) for the owner eyeball. Queue re-groomed: flashlight-orb → #1 (spec now committed),
pixel5-variants → #2 as a SCHEDULED overnight one-shot (run #6 handoff rule), slice 7 seeded
(shimmer baseline table). Next run: flashlight-orb + slice 4 → demo/gif regen.

## Late evening — owner catches the relit energy bug ("bloom with extra saturation")
Owner (eyeballing smoothed heroes): relit bumps saturation/vividness, creates "made-up shadows
and light areas", "something set to 1 instead of a subtle value". Diagnosis CONFIRMED same
hour: `relight.glsl:121` applies the env-SH ambient at weight 1.0 (ignoring the 0.2 slider) —
but the sidecar is the FULL capture illumination. Audit (both heroes): env ambient = 4.2–4.4×
design; 23–27% of splats clip a channel under the unit sun (vs ~0.5–0.9% flat); +31% mean
saturation on pxl_144634. DC-normalizing to the slider restores the budget exactly while
keeping env shape/tint. NOTE: this also retro-explains the owner's 2026-07-14 "saturated"
report — the albedo-side refutation stands (albedo IS clean), but the relit-side energy was
the real signal in what they saw.
Seeded `tasks/2026-07-15-relit-energy.md` as Ready #1 (before flashlight-orb — same seam).
Planner shipped viewer diagnostic toggles (V=envSH/flat, A=sun-off, D=ambient-off) + verified
launch on pxl_131945. Audit script preserved in task numbers; scratch: energy_audit.py.

## Night — owner catches defect #2: normal sign domains ("splats not synchronized in angle")
Owner, using the new D toggle (sun-only): patchy fake shadows on leaves, suspects orientation/
precision math. Audit (full-res k-NN, both heroes, smoothed + .bak unsmoothed): (1) decompose
output has ~29% sign-opposed neighbor normals (half of all incoherence = pure sign flips;
camera-hemisphere orientation not surviving the solve); (2) sign-naive smooth_normals_knn
majority-voted them into randomly-signed DOMAINS ~0.05–0.1 units (8-NN opposition 1% but
18.6–20.2% at 0.11-unit scale) — the visible patch shadows; (3) 8.9–10.0% splats shipped
renormalized near-cancellations; (4) the local_coherence tripwire is signed → rewarded domain
formation. Seeded `tasks/2026-07-15-normal-sign-consistency.md` READY #2 (M3 prerequisite —
dot(−N,L) needs signs). Queue: relit-energy #1 → sign-consistency #2 → flashlight-orb #3 →
pixel5 #4 (after #2 so variants inherit clean normals). Audit scripts in scratchpad; numbers
preserved in the task file. Owner also confirms FPS readout great, perf comfortable.
