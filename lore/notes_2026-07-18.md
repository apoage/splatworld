# Planner notes — 2026-07-18

Prev: `lore/notes_2026-07-17.md`. Handoff chain head: `lore/handoff_2026-07-17_d7-signed-sandbox-synthetic.md` (#9).

## Reconcile — dark-factory run #11 (M3 transmission code, v0.20.0)

Factory disarmed 06:51 (was armed 2026-07-17 22:12 for the M3 build). Morning reconcile done:

- **Handoff #9 committed** (`41846e2`) — had been left untracked at chandoff because the git gate
  blocks planner commits while the factory is armed; landed on disarm as designed.
- **Verified before push**: independent `pytest precompute/tests` = **120 passed** (matches the
  factory's claim); secret/endpoint scan of the public delta (`origin/main..main` + handoff #9)
  CLEAN (no trader SSH endpoint, creds, or GPS/exif); no raw-data / >5 MB / `.ply`·`.blend`·`.mp4`
  blobs in the delta — pure code+docs.
- **Pushed**: `main` → `c529eef`, tag `v0.20.0` published. origin synced 0/0.
- **Groomed QUEUE**: M3 row struck (code done); milestone *acceptance* moved to Parked/owner-gated
  (hero re-export + a-vs-b eyeball + GPU backlit check); demo/gif regen re-sequenced AFTER the a/b
  pick; runs #8–#11 folded into the shipped-runs block; pixel5 drift note (`--stages all` now
  appends `transmission`, so overnight variants inherit leaf `trans=0.5`).

## Read of where the project stands

The Moon-Stone runtime stack is now **feature-complete in code**: relight ✓ · env-SH ambient ✓ ·
point-light/flashlight ✓ · transmission ✓. That's the milestone-chain payoff — every runtime piece
the release demo needs exists behind a toggle.

Consequence for the factory queue: the Ready section is now **thin** — no big milestone row is
factory-takeable until (a) the owner's M3 a/b eyeball lands (unblocks demo/gif regen) and (b) M4
opens (needs pixel5 variants + the owner's Moon-Stone M4 spec: point lights in the pass are done,
but day/night state + touch-trigger fireballs are new rows). What's left for the factory right now
is filler/scheduled: quality-pass slices 6/7, the pixel5 overnight one-shot, docs-guide.

Two independent tracks are teed up and BOTH owner/planner (not factory):
1. **M3 acceptance** — owner eyeball (the honest milestone gate).
2. **Synthetic-plant-GT** — de-risks the D7 photometric sign-recovery research (M-0 reflectance
   histogram); `plant01` already rendered; next step = reconstruct via pipeline → transfer GT sign.

Neither is autonomously advanceable without the owner's call on which to pursue, so I'm parking
here after the reconcile rather than starting new work.

## Afternoon — cactus sandbox, GDGS bug, M4 design, run #12 reconcile

- **Cactus in the sandbox.** Built `precompute/tools/vanilla_to_relight.py` (any vanilla 3DGS →
  neutral .relightply: albedo=SH0, shortest-axis normal oriented outward/up/raw, const rough/trans/
  label, EXPLICIT `--coord`, optional D5 smoothing). `cactus_142k.relightply` = `--coord colmap
  --normal-orient outward` unsmoothed. **Orientation lesson: the INTERACTIVE VIEWER is the arbiter**
  — `render_sparkle` showed `--coord none` upright but the viewer showed it upside down; flipped to
  `--coord colmap` to match the viewer. The render↔viewer discrepancy cause is UNRESOLVED (may bite
  demo render tools vs in-viewer later). Normal smoothing HELPS planar foliage, HURTS spiky (cactus
  coherence 0.725→0.709; owner "worse") → off for high-freq. Moving-light "traveling spots" =
  intrinsic per-splat reshading limit (owner-diagnosed); only lever = a shading-model terminator-
  softening A/B toggle (offered, not built).
- **GDGS fullscreen/zoom tile-dropout — root-caused + FIXED.** Investigation `wf_93bf8a1c-a50`
  (HIGH conf): GDGS sizes radix-sort pair buffers by `point_count*10` (resolution-independent),
  unchecked `atomicAdd` OOB at fullscreen/zoom → dropped tiles = background holes. Report
  `docs/2026-07-18-gdgs-tile-dropout-report.md` (doubles as the upstream bug report/PR, owner-gated).
  Factory run #12 shipped **v0.21.0** (`8030116`): 3-file logged vendored diff (resolution-aware
  pair budget + ping-pong stride + shader clamp) + `render_probe.gd` empirical proof (794 holes → 0,
  3.3× headroom, causal revert). Vendored-diff entry now in `docs/decisions.md` (invariant #6).
- **M4 DESIGNED** (`wf_ed5f9c8a-f62`) → `tasks/2026-07-18-m4-carpet-authoring.md`. Owner steer:
  AUTHORING TOOLS not an auto-scene builder. Contract-First hybrid (Godot in-viewer scatter *Splat
  Studio* PRIMARY + Blender bpy addon SECONDARY + `instances.json` contract; cleanup = Godot-select→
  Python-write via `clean_relight.py`, which also mints the ≤1.5M variant fleet; net-new runtime =
  `RelightPass.set_materials_multi`; GDGS untouched). 7 walls bundled as **DECISIONS D8** — the M4
  spine (set_materials_multi + carpet_loader) is testable NOW on the 2 heroes but D8 must be ratified
  before Splat Studio / cleanup / Blender addon. Factory correctly STOPPED at the D8 wall.
- **Run #12 reconcile:** creds/GPS/large-file scan clean; `docs/decisions.md` entry appended;
  committing the M4 grooming + this note; pushing main + tag v0.21.0.

## Evening — D8 ratified, README refresh, run #13 reconcile

- **D8 RATIFIED** (owner "D8 yes → green for factory"). Moved to DECIDED; M4 spine became
  factory-takeable → Ready #1. Upstream GDGS PR parked pending owner cross-validation.
- **README refresh** (`91d1d29`, pushed): owner flagged the GitHub README as stale ("old sparkly
  blobs"). Reframed as a **live, under-development testing environment** (owner steer — A/B toggles
  visible on purpose). Owner recalibrated my over-selling: his foliage splats are "horrible from a
  model-quality perspective," these are "hey it's somewhat working" shots, not beauty shots → saved
  memory `feedback-honest-framing-not-hype`. Result: **cactus (clean CC0) carries the "watch it
  relight" story**, foliage shown honestly as the rough real target. 7 real relight shots copied to
  `docs/img/` (metadata-scanned clean), M0–M4 status corrected. Old sparkly `m1_foliage_*` + the
  July-12 orbit gif dropped from the README.
- **Run #13 reconciled + pushed** (`3c97883`, tags v0.22.0/v0.23.0): M4 spine
  `set_materials_multi`+`carpet_loader.gd` (v0.23.0) + `clean_relight.py` decimator (v0.22.0).
  Independent pytest **141✓**, delta scan clean. The material-concat coupling (shader `materials[si.y]`
  ↔ registry first-seen order) verified on all hard cases. **NEW finding → seeded DECISIONS D9**
  (mixed-scene buffer ownership; gated to Moon-Stone). Queue: M4 tasks 1+2 struck; **task 3 perf** is
  the next factory-completable slice (build harness → scheduled GPU one-shot); tasks 4/5 owner-attended.

## Owner-gated / next
- **Steer run #14:** (1) perf task 3 as a scheduled GPU one-shot (rec: yes, cleanest next slice —
  the 60fps number gates whether authoring UI is worth building); (2) authoring tasks 4/5 — rec: hold
  Splat Studio until the perf budget is in hand + build it owner-attended.
- **Upstream GDGS report/PR** — parked pending owner cross-validation.
- Open threads unchanged: M3 a-vs-b eyeball; render↔viewer orientation discrepancy; plant→splat
  reconstruction (transforms→COLMAP converter scoped, not built).
