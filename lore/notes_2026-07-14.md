# Planner notes — 2026-07-14

## Pre-run green check
Gates re-verified (45 tests + SMOKE OK 39s), both 2026-07-13 handoff lore files committed +
pushed, viewer uid committed. Confirmed the smoke's 48k point count is its OWN 400-step
artifact (gitignored out-root), not a recurrence of the train_base clobber — real assets
untouched. Verdict GREEN; owner armed the factory at 19:26 (run #4).

## Owner requests while the run was live (planner lanes only)
1. **Lighting-stability testing** — "we have delighting and a sort of weird internal lighting
   engine — test it so lighting is stable on multiple conditions with multiple engine lighting
   models." Seeded `tasks/2026-07-14-lighting-stability.md` (Ready #2 post-reconcile):
   condition-matrix render harness with hard asserts (raw invariance, energy linearity,
   360° return, elevation smoothness, ambient floor, trans inertness) + Godot-lit reference
   sphere cross-model check + shimmer baselines (gate stays with normal-quality step 2).
2. **Lighting-lab viewer** — drafted while lanes were closed (lane guard correctly blocked the
   direct edit → draft parked in prototype/), shipped into
   `godot/relight/tools/orbit_viewer.gd` after disarm: right-drag sun, presets
   noon/golden/overcast/moonlight/backlit, day-cycle (C), live energy/ambient/wrap keys.
   Prototype draft deleted after shipping.
3. **Unreal port** — ON HOLD, parked row in QUEUE: Epic MegaGrants candidate ("why not
   eventually"); no work/research until owner reopens.

## Run #4 reconcile (disarm 21:46, caught by a state.json monitor)
Shipped: **v0.11.0 ground-alignment** (camera-ring up estimate composed into the single
export conversion; env-SH rotates identically; suite 45→76) + **v0.12.0 normal-quality
step 1** (sparkle = spatial neighbour-normal incoherence — NOT sort/aliasing, NOT floaters;
render-free shimmer metric `precompute/tools/gaussian_twinkle.py`, baseline 197.53; verify
round killed an 8-bit-PNG-quantization confound in the first metric). Gates re-run (78 tests,
SMOKE OK), creds + large-file scans clean, pushed + tags. Details:
`docs/2026-07-14-handoff-4-run4.md`.

**Owner-input surface left by the run:**
- pxl_144634 exports `alignment_suspect=true` (43.5° plane-normal vs camera-up split) —
  owner eyeball in the viewer decides; cue can switch to `mean_camera_up_colmap` without
  re-decompose (both candidates recorded in `metrics_export.json > alignment`).
- D2 still the one open word (rec: 500k + opacity-0.02 prune).
- normal-quality step 2 go/no-go (corrected gate: shimmer ≤98.8 AND held-out PSNR ≤1.5 dB
  AND anti-over-smoothing guard — shimmer alone is gameable).

Grounded assets + env-SH sidecars re-mirrored into `godot/gs_assets/` (stale Jul 12 mirror
replaced); viewer launched for the alignment eyeball.

## Late — orientation eyeball PASSED, gates opened
Owner re-eyeballed the fixed viewer: "orientation seemed alright" — the v0.11.0 alignment was
correct all along (the 43.5° `alignment_suspect` split was a noisy camera-up cue, NOT a wrong
plane-fit; flag mechanism stays). With D2 decided the pixel5-variants gate is fully open →
Ready #3. Saturation report resolved as refuted (data in decisions.md); viewer HUD now shows
the active mode. Queue for run #5: normal-quality step 2 (owner GO) → lighting-stability →
pixel5-variants.
