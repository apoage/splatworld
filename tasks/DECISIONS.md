# DECISIONS — the single blocked-on-human surface

One row per call. Recommendation FIRST — the owner should be able to answer with one word.
OPEN rows are walls: the factory never guesses past one; the planner seeds rows here instead of
scattering open questions across task files. Answered rows move to DECIDED with the date.

## OPEN

| # | Decision | Recommendation | Context |
|---|----------|----------------|---------|
| D10 | **Dark-factory MULTILINE** — the model-failover architecture (owner-decided **B route**: Claude Max orchestrates-only + disposable worker-agent lines on other providers over MCP + main handles worker death/reroute; NOT A route = today's "GLM in Claude Code with df", which can't handle death). Two sub-choices remain OPEN: **(i) worker harness** — `opencode+opencode-mcp` vs OpenHands; **(ii) build go / first slice.** | **(i) opencode+opencode-mcp first** — the only off-the-shelf "agent over MCP" (run/fire/reply/check = dispatch/poll), fleet-wide via models.dev; the §6 gate-immutability BEAM already provides isolation so OpenHands's sandbox is redundant for v1. **(ii) BUILD a thin vertical** — one GLM worker line + the L2 lease/worktree/circuit-breaker state machine + the git-restore gate beam, proven on ONE real task = the Splat Studio **Paint fix** (`tasks/2026-07-19-splat-studio-followups.md` #1); then add Kimi/Qwen-local lines + judge panel + the Colibri async-judge experiment. Non-learned static-priority router; WAIT-not-escalate; gate stays pure pass/fail. | Full design + research: `docs/2026-07-19-dark-factory-multiline-design.md` (runs `wf_42efecd4-d5a`, `wf_358bd85a-615`). Proven by the GLM v0.25.0 run (worker died on a cap → this is the fix). Ties to [[project-splatworld-source-of-truth]] |
| D9 | Mixed-scene material-buffer ownership: `RelightPass.set_materials_multi` OVERWRITES the one global material buffer with only the carpet's variants, and the shader indexes `materials[si.y]` by GDGS's global first-seen (node add_child) order. A scene with the carpet **plus any other registered `GaussianSplatNode`** (e.g. Moon-Stone: carpet + a standalone relit hero) shifts every `si.y` → whole-scene mis-shade. Run-#13 finding (documented in both docstrings, NOT enforced in code). | **Two-part:** (a) NOW, cheap insurance — add a fail-closed **registry-count assert** in `set_materials_multi` (concat entry count must equal the registry's unique-resource count) so a mismatch refuses loudly instead of silently mis-shading; (b) WHEN a real mixed scene lands — a **per-node material-offset scheme** (each node carries its own base offset) so carpet + hero coexist. Until then the precondition holds: "the carpet loader owns ALL registered splat nodes; fully remove the prior carpet before a rebuild." **Gated to the first mixed scene (Moon-Stone) — not a wall for tasks 3–6, which are carpet-only.** **Task 4 (Splat Studio) single-carpet `resync_materials` stays INSIDE this precondition and does NOT resolve D9 — do NOT fold the D9 assert / per-node-offset work into that task.** | run #13 handoff `docs/2026-07-18-handoff-run13-m4-spine-decimator.md`; `relight_pass.gd:155` + `carpet_loader.gd` docstrings; ties to [[project-moonstone-meadow-demo]] |
| D3 | M4 orientation convention: how to resolve the GDGS identity-vs-scatter-basis 180°-about-Z inconsistency? | M4 carpet ALWAYS sets an explicit node basis (never leaves a splat node at identity) so GDGS's conditional default −180° Z correction never fires and all instances share one convention — do NOT re-derive the export matrix (it's the correct pure change of basis). **2026-07-14 evidence:** the conditional correction FIRED on the grounded v0.11.0 asset (owner: renders "180° upside down"); `relight_controller.gd` now sets identity post-`add_child` per this row's rule, remaining `.relightply` tools = quality-pass slice. **SETTLED + IMPLEMENTED by the M4 spine (v0.23.0, run #13): `carpet_loader` sets each node transform AFTER `add_child` per this rule (verified on the hard cases). Non-blocking for tasks 3–6; kept here only as the convention of record.** | code-hardening item 14; `precompute/core/ply_io.py:51-68` NOTE; decisions.md 2026-07-12 + 2026-07-14 entries |

## DECIDED

| # | Decision | Outcome | Date |
|---|----------|---------|------|
| D8 | M4 authoring approach + its 7 sub-walls (D-AUTHORING-HOME, D-INSTANCES-CONTRACT, D-SCALE-POLICY, D-MATERIAL-CONCAT-OWNERSHIP, D-ENV-SH, D-CLEANUP-COMMIT-FORMAT, D-BLOCK-CULLING) | **RATIFIED — Contract-First hybrid (owner "D8 yes → green for factory" 2026-07-18).** One "yes" ratifies all 7 sub-walls at their spec defaults: Godot in-viewer scatter *Splat Studio* PRIMARY + Blender bpy addon SECONDARY + `carpet/<name>.instances.json` contract; TRS-only uniform scale; cleanup = Godot-select→Python-write via `clean_relight.py` (which also mints the ≤1.5M variant fleet); one global env-SH; EditorImportPlugin deferred; net-new runtime = `RelightPass.set_materials_multi`, GDGS untouched. **The entire M4 spine is now factory-takeable** — QUEUE Ready #1 (was #2). Spine (set_materials_multi + carpet_loader) was already dependency-free; the walls that were gating Splat Studio / cleanup mode / Blender addon are now open. Sub-wall defaults live in `tasks/2026-07-18-m4-carpet-authoring.md`; any later per-wall override is a new DECISIONS row, not a re-litigation. | 2026-07-18 |
| D7 | Grazing-normal sign: which runtime shading? (prototype v0.19.0 A/B'd live) | **KEEP SIGNED (mode 0, shipped default) — owner eyeball 2026-07-17 OVERRIDES the 4-report "go sign-agnostic" consensus** (empirical arbiter per the validation stance). Verdict on pxl_144634 sun-only: signed = strongest shadow force + best self-cast/shadow-clustering (still noisy); sign-free WRAP = shadow force too weak (washes out depth); flip-to-cam = noisier. The ~30% wrong-sign splats give closeup "psychedelic" salt-and-pepper (rotated splat reads as shadow amid front-facing lit neighbors); **owner ACCEPTS this as a property** — "epic" at middle distance (our target), psychedelic only at closeup. Consequences: (a) runtime stays signed; the v0.19.0 sign modes stay as diagnostic tools, not the default. (b) **M3 backlit `dot(−N,L)` is UN-BROKEN** (only degenerate under sign-agnostic) — original plan holds, but wrong-sign splats will mis-glow ~30% under backlight → watch for the Moon-Stone fireball money-shot (note in M3 spec). (c) The offline **photometric sign-recovery track is ELEVATED not abandoned**: with signed shading kept, getting the ~30% signs RIGHT (Q-C, `prototype/open-problem-global-normal-sign.md`) directly reduces closeup noise WHILE keeping the shadow structure the owner likes — best of both. (d) real remaining problem reframed = closeup noise (angular error + registration + temporal sun motion), studied via the sandbox now being built. **(e) 2026-07-24 DEBT (SuperSplat cleanup round-trip):** the cleaned heroes (`train_base_clean.ply`, pxl_144634 −42% / pxl_131945 −50%) re-decompose FINE on PSNR (pxl_144634: re-render 17.3 dB heldout / 20.7 masked ≥ cleaned baseline 15.4, within the 1.5 dB drop budget) but trip the decompose `--max-sign-opposition` gate at 8-NN 29.25% (domain 32.44%). That gate (5% default, added v0.18.0) is STRICTER than this row's accepted reality — the shipped heroes predate it (their `metrics_decompose.json` has no `normal_sign` block) and D7 explicitly ACCEPTS ~30% signed opposition as a middle-distance property. So cleaned heroes v0.27.0+ ship with `--max-sign-opposition 0.35` (parity, not new debt). **Open follow-on:** the 5% decompose gate default should be reconciled with D7's acceptance (raise the default, or make it advisory not fatal) so honest re-decompose doesn't need an experiment flag — seed a task when the cleanup track is next touched. **(f) 2026-07-24 owner eyeball on the CLEANED pxl_144634 (1.41M, SuperSplat -42%):** "gorgeous"; sign-free WRAP now reads "least aggressive" but ALL modes usable — a SOFTENING from this row's 2026-07-17 full-cloud verdict (where wrap "washed out depth, shadow force too weak"). Working hypothesis: cleanup pruned floaters/background that amplified the wrong-sign salt-and-pepper, narrowing the mode gap. Not a decision change (signed stays the shipped default), but evidence that the closeup-noise problem (consequence d) is partly a cloud-hygiene problem, not only a normal-sign problem. | 2026-07-17 (e,f: 2026-07-24) |
| D6 | Scalable grazing-normal sign resolver (camera-hemisphere proven insufficient at 29.6%/31.0% vs 5% gate) | **HYBRID (owner "yes")**: (a) visibility-weighted orientation accumulated over ALL contributing training views during the solve + (b) coarse-voxel sign-field propagation for the grazing residual; both O(N). Task: `tasks/2026-07-16-grazing-normal-resolver.md` (includes the two gate defects found operating it: rc=0 fail-open exit on FATAL refusal; refusal clobbers tracked metrics). Target: multi-scale opposition <5% on both heroes ⇒ patch shadows largely gone; unblocks M3. **→ IMPLEMENTED in v0.18.0 but the hybrid was REFUTED on real data (~30% floor, balance-invariant); superseded by the OPEN D7 row.** | 2026-07-16 |
| D2 | Final per-asset Gaussian budget for foliage blocks? | **Option (a) — 500k @ ~19.5 dB with opacity-0.02 prune** (owner 2026-07-14: "sounds good"). Foliage PSNR floor relaxed per the v0.5.0 tradeoff data (≤500k @ ≥20.7 dB was unachievable — 20.7 dB needs ~1.1–1.2M); many cheap instances over few expensive ones, fits the ≤1.5M whole-carpet target. Full curve: `docs/validation-perf-budget-2026-07-12.md`. Unblocks pixel5-variants (still needs the grounded-orientation eyeball). | 2026-07-14 |
| D5 | Decompose normal quality (near-isotropic ‖mean‖≈0.20) | **FIX before M3** — owner eyeball (2026-07-13): relighting reads good, but per-splat SPARKLE during the orbit "would be bad for usable render" and small-arc response is weak. Task: `tasks/2026-07-13-normal-quality.md` (diagnose sparkle attribution first, then anisotropy/smoothing/confidence-clamp in decompose). M3's dot(−N,L) backlit term depends on this. | 2026-07-13 |
| D4 | Recovered env light → runtime ambient? | **YES** — wire the Godot `ambient_sh(N)` reader (`env_sh.json` sidecar, constants shared with `core/sh_env.py`); sequence after M2b phase D. Task: `tasks/2026-07-12-env-sh-runtime.md` | 2026-07-12 |
| D1 | Inverse-rendering impl for `decompose` | **GI-GS** (github.com/stopaimme/GI-GS, MIT, ICLR 2025), hybrid vendor+port: vendor its MIT Python layer (losses/training/materials), re-host G-buffers on gsplat, pure-PyTorch env light, drop indirect pass v1. NEVER vendor its Inria rasterizer fork or nvdiffrast. Everything authored stays Python/PyTorch — no new languages. Owner note: partial reimplementation accepted. Evidence: `docs/d1-survey-2026-07-12.md` | 2026-07-12 |
| D0 | Compute env + SfM approach | Dev on local 3090 (cu124, sm_86); batch on trader 4×3090; `ingest` = fresh COLMAP SfM (no dataset ships usable poses) | 2026-07-11 |
| D0b | Render host + Godot version | Vendor GDGS @ `be61f8f` (v2.2.0) into `godot/addons/gdgs`; Godot **4.7** (needed a push-constant patch, logged in decisions.md) | 2026-07-11 |
| D0c | train_base implementation | Compact self-contained trainer on gsplat core API (`rasterization` + `DefaultStrategy`), not the heavy gsplat examples (nerfview/pycolmap/numpy<2 conflicts) | 2026-07-11 |
