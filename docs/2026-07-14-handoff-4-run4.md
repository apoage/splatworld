# Dark-factory run #4 handoff — ground-alignment + normal-quality diagnosis (2026-07-14)

Prior runs: `docs/2026-07-12-handoff.md` (#1) · `-handoff-2-M2.md` (#2) · `-handoff-3-run3.md` (#3).
This is #4 (owner "run factory" over the 2026-07-13-groomed queue: ground-alignment, then normal-quality).

**Branch:** main · **WD:** /home/lukas/splatworld · **Factory:** disarmed at end · nothing pushed
(`allow_push:false`; owner pushes after review).

## Shipped this run (each verified by an independent adversarial panel — never self-reviewed)

| version | item | verification outcome |
|---|---|---|
| **v0.11.0** | **ground-alignment** — `export` estimates world-up from the camera ring (`core/orient.py`) and composes it into the SINGLE COLMAP→Godot conversion (`C = M @ R_align`); env-SH sidecar rotates identically (real deg-2 SH rotation). `--sparse`/`--no-align`/`--strict-align`. | 6-lens panel → **2 MAJOR + several MINOR fixed** → 2 re-verify rounds → final **0 BLOCKER/0 MAJOR**. sh-rotation-math + regression + security CLEAN throughout. Suite 45→76. |
| **v0.12.0** | **normal-quality DIAGNOSIS (D5, step 1 of 2)** — attributes the orbit sparkle: **SHADING class = spatial neighbour-normal incoherence**. Sort/aliasing + floaters ruled out. New render-free per-Gaussian **shimmer** metric (`precompute/tools/gaussian_twinkle.py`), baseline 197.53. | Verify round 1 found a **BLOCKER** (8-bit PNG quantization confounded the first metric) → rebuilt render-free/float → correctness judge reproduced all numbers, **REFUTED circularity**, drove gate-framing + tool corrections → re-confirmed PASS. Suite 76→78. |

## Decision-changing findings (the owner-input surface)

1. **ground-alignment: pxl_144634 flags `alignment_suspect=true` (43.5° plane-normal vs camera-up split).**
   On a symmetric walkaround those cues should ~coincide, so the plane-fit "up" may not be gravity there.
   The code applies alignment by default (with a loud warning + both candidate ups recorded); whether the
   asset reads level is an **owner eyeball**. pxl_131945 is not suspect (22.0°). Seed: `metrics_export.json
   > alignment` has `up_colmap` (plane-normal) + `mean_camera_up_colmap` so the cue can be switched without
   re-running decompose.
2. **normal-quality: the sparkle is real and normal-driven (not renderer/sort, not floaters).** The fix is
   viable and cheap in preview (numpy k-NN normal smoothing drops shimmer −75%, appearance-stable) — but the
   **step-2 gate was corrected**: `shimmer ≤ 98.8` is necessary-NOT-sufficient (gameable by over-smoothing),
   so step 2 MUST also validate held-out re-render PSNR (≤1.5 dB) on the smoothed/shipped normals + an
   anti-over-smoothing guard. Step 2 is an **expensive-real-data unit** (re-decompose/re-render) — not built.

## Where it stands / open items

- **Ready (next unit):** **normal-quality STEP 2** (the fix). Candidate first move = export-time k-NN normal
  smoothing OR a decompose-side neighbour regularizer, gated per the corrected gate above. Needs a real
  re-render/re-decompose validation → schedule as its own run.
- **OPEN DECISIONS:** **D2** (foliage budget — gates pixel5-variants + M4), **D3** (M4 orientation, gated to M4).
- **Deferred (owner-eyeball-gated):** ground-alignment demo/gif regeneration on the grounded asset +
  `godot/gs_assets` re-mirror (premature until the pxl_144634 alignment is confirmed correct).
- **Filler untouched:** recurring-quality-pass (the 48k train_base.ply clobber root-cause; M2a MINORs),
  docs-guide.
- **Pre-existing untracked planner file** `prototype/orbit_viewer_lightlab.gd` left as-is (not factory lane).

## The 1–2 questions that unblock the highest-value track

1. **Eyeball the two grounded assets in the viewer** (esp. pxl_144634 @ 43.5°). If it does not read level,
   switch the up cue to `mean_camera_up_colmap` (or the optional EIS-gyro path) — evidence is in the metrics.
2. **Confirm D2** (per-asset foliage budget) so pixel5-variants (M4 shortlist) can build on a grounded asset.
3. (Go/no-go) **normal-quality step 2** — proceed with export-time normal smoothing + a real PSNR re-validation?

## Git state (safety-net)

```
$ git log -4 --oneline
3d55acd normal-quality: sparkle diagnosis + render-free shimmer metric (v0.12.0, step 1 of 2)
13b5c83 ground-alignment: estimate world-up from the camera ring, compose into export's single conversion (v0.11.0)
1c0483d Godot uid for orbit_viewer.gd
311e8f4 Handoffs #3 (factory run3) + #4 (planner viewer-feedback arc)
```
Tags v0.2.0 … **v0.12.0**. Factory disarmed. Nothing pushed. Suite: 78 passed.

## Next action

Next run's top track is **normal-quality step 2** (the D5 fix) — implement export-time k-NN normal smoothing
(or a decompose neighbour regularizer), then RE-RENDER + validate held-out PSNR ≤1.5 dB on the smoothed
normals (the shimmer gate alone is gameable). In parallel, the owner eyeball on the two grounded assets +
D2 confirmation unblock pixel5-variants → M4.
