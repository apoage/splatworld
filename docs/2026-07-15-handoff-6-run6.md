# Dark-factory run #6 handoff — lighting-stability (shipped) + pxl_131945 D5 rollout (shipped)

Prior runs: `docs/2026-07-12-handoff.md` (#1) · `-handoff-2-M2.md` (#2) · `-handoff-3-run3.md` (#3)
· `docs/2026-07-14-handoff-4-run4.md` (#4) · `docs/2026-07-15-handoff-5-run5.md` (#5). This is **#6**
(owner re-invoked the factory over the run #5-groomed queue: lighting-stability → slice-5 tail).

**Branch:** main · **WD:** /home/lukas/splatworld · **Factory:** disarmed at end · nothing pushed
(`allow_push:false`; owner pushes after review).

## Shipped this run (verified — never self-reviewed)

| version / commit | item | verification |
|---|---|---|
| **v0.14.0** (`b866991`, tag v0.14.0) | **lighting-stability harness** — finished the run #5 WIP `godot/relight/tools/render_matrix.gd` into a legitimate, repeatable **10/10** gate (53-condition matrix, `DISPLAY=:0`). | correctness + regression + flow-verifier. Correctness judge caught the first pass having **lowered two PSNR floors below their measured worst on a false rationale**; fix pass set RAW 50 / TRANS 55 with real margin, corrected the comments, de-duped trans self-compares (n_pairs 28→11). Flow-verifier fault-injection proved the loosened check still fires (2% leak = 36 dB FAIL vs ~56 dB clean). Same judge re-confirmed RESOLVED, no new issue. Suite 88. |
| **data rollout** (`d766956`, no version bump) | **slice-5 D5 rollout → pxl_131945** (2nd hero asset). Re-decomposed with `--smooth-normals-iters 2`, promoted to `assets/built` + mirrored to `gs_assets`. | flow-verifier + correctness. Gate passed (held-out **24.202 dB** ≥ budget 23.72); shipped bytes confirmed smoothed (coherence **0.925** vs backup 0.576); albedo/rough byte-identical (cost is normals-only); counts consistent (2,075,806); source untouched. Suite 88. |

## Decision-changing / owner-input surface

1. **pxl_131945 smoothed asset — OWNER EYEBALL RECOMMENDED before treating as final.** The correctness
   judge raised a MINOR advisory: the smoothing cost **0.50 dB** is 7.5× pxl_144634's 0.067 dB and the
   gate headroom is thin (0.48 dB) — it passes every defined gate (budget_ok, over_smooth_suspect false,
   coherence 0.925 < 0.985) and the material channels are byte-identical (so it is genuinely normals-only,
   not a degraded decomposition), but it is "riding the gate." End-state coherence (0.925) matches the
   owner-approved pxl_144634 (0.922), which is what governs sparkle, so it should look equally clean —
   but this is the second hero asset the owner will see. **Trivially reversible:** originals are at
   `assets/built/pxl_131945/{decompose_unsmoothed.ply.bak, env_sh_unsmoothed.json.bak}` (gitignored) +
   `git checkout` the 4 tracked JSONs. The validated smoothed scratch is at `.perf/normalsmooth/pxl_131945/`.
2. **The viewer now loads the smoothed pxl_131945** (`godot/gs_assets/pxl_131945.relightply`) — so the
   owner can re-eyeball both hero assets sparkle-free in the lighting-lab viewer.

## Not taken this run (with why)

- **Ready #3 pixel5-variants** — build ≥5 fresh variants from `datasets/pixel5/` clips (data present). This
  is the largest queue item (full pipeline per clip: SfM → train_base → decompose → export, potentially
  hours). Per the validation-tier rule (expensive real-data work = SCHEDULED overnight/idle one-shot, not
  in-loop grinding), it should be a scheduled one-shot with a dated findings doc. Decompose fresh with
  `--smooth-normals-iters 2` to bake in D5. NOTE: hero-asset budgets keep full splat count; the D2 500k
  budget is for M4 CARPET blocks — apply the 500k + opacity-0.02 prune to the *variant* exports per D2.
- **Ready #4 flashlight-orb** — the planner seeded a COMPLETE spec (`tasks/2026-07-15-flashlight-orb.md`) this
  run, but it was still **uncommitted** (untracked) at run end, and it is a shading-contract change (first
  since M2a — touches `relight.glsl` + `relight_pass.gd::set_light` + `orbit_viewer.gd`). Starting a
  contract-level change against an uncommitted spec is unwise; it deserves a fresh focused run with
  high-tier verification once the planner reconciles the spec. Its item 2 FPS/frame-time HUD readout
  ("do FIRST, independent of the flashlight") is a good low-risk warm-up. Owner explicitly requested this
  after the D5 eyeball ("more aggressive lighting like flashlight… reference orb would be great").
- **slice-5 demo/gif regen** — gated on **slice-4** (GDGS conditional −180°Z neutralization in the remaining
  `.relightply`-loading render tools: render_orbit, render_sparkle, relight_render_gate, render_probe — set
  `transform = Transform3D.IDENTITY` after `add_child` per the D3 rule; render_matrix.gd + relight_controller.gd
  already do this). slice-4 is bounded Godot code and is the natural unblock for showing the smoothed hero
  assets in fresh demo footage.

## Where it stands / suggested next run

Highest-value next unit is a coherent Godot code pass: **slice-4** (GDGS −180°Z neutralization in the 4
remaining render tools) → **slice-5 demo/gif regen** on the now-smoothed grounded hero assets (the visible
payoff of D5). Then **flashlight-orb** (once the planner commits its spec) as the top feature — Moon-Stone
point-light prerequisite. **pixel5-variants** is best run as a scheduled overnight one-shot.

## The 1–2 questions that unblock the highest-value track

1. **Eyeball the smoothed pxl_131945 in the viewer** (like you did pxl_144634) — does it look clean, or should
   it be reverted to unsmoothed? It rode the gate closer than pxl_144634 did (0.48 dB headroom). Reversible.
2. **Finalize/commit the flashlight-orb spec** so the factory can take it next run, or re-prioritize slice-4 +
   demo/gif regen (the visible D5 payoff) ahead of it?

## Git state (safety-net)

```
$ git log -4 --oneline
d766956 slice-5: roll out D5 normal-smoothing to pxl_131945 (2nd hero asset)
b866991 lighting-stability: finish render_matrix.gd to a repeatable 10/10 gate (v0.14.0)
173e12f Handoff #6 (factory run5): v0.13.0 D5 fix + lighting-stability WIP + slice-5 rollout state
489de5d Slice-5 rollout artifacts: smoothed decompose+export metrics for pxl_144634; ...
```
Tags v0.2.0 … **v0.14.0**. Factory disarmed. Nothing pushed. Suite: 88 passed.

**Concurrent planner activity (NOT the factory's):** at run end the working tree also carried
`lore/notes_2026-07-15.md`, `tasks/QUEUE.md` (modified) and `tasks/2026-07-15-flashlight-orb.md` (untracked)
— the concurrent planner thread's uncommitted grooming (Moon-Stone survey, flashlight-orb spec, slice-6
seed), which it commits at its own reconcile. The implementer lane is clean; those files are outside it.

## Next action

slice-4 (bounded Godot code) → slice-5 demo/gif regen on the smoothed hero assets → flashlight-orb (after the
planner commits its spec). pixel5-variants as a scheduled overnight one-shot. M3 (transmission) remains
unblocked (normal-quality step 2 shipped v0.13.0).
