# 2026-07-12 — handoff #2: M2 build (continuation of the armed dark-factory run)

After the owner's "green go", the factory built the M2 milestone. Combined with handoff #1
(`docs/2026-07-12-handoff.md`, v0.2.0–v0.5.0), this session shipped **v0.2.0 → v0.7.0**.
Stopped before M2b phase D by design — see "Why stopped".

## Shipped this continuation (banners on task files)

| ver | item | outcome | verification |
|---|---|---|---|
| 0.6.0 | **M2a** relight runtime (godot/) | extended-PLY importer + a ONE-SEAM relight compute pass into GDGS + `single_asset.tscn` (fixed the `--import` error) + two gates. Render gate on the 3090 proves relit≠raw (\|ΔL\|=0.335), orbit changes shading, ambient floor holds. | design workflow (mapped GDGS, vetted the single insertion) → implement → correctness+regression+flow panel. GPU byte-compare confirmed the OFF-path byte-identical. |
| — | M2b **Phase A** build-verify | GI-GS builds+trains on our sm_86/cu124/py3.11; full env recipe + the architecture the blind port reproduces. | license/cleanliness gate (scaffold/ gitignored, zero tracked). Doc, no version bump. |
| — | M2b **Phase B** gsplat G-buffer | `test_gbuffer_smoke.py` proves gsplat renders the 8-ch feature G-buffer + depth with gradient flow (clears D1 risk #3). | correctness judge (mutation-tested, non-tautological). Test-only, no bump. |
| 0.7.0 | M2b **Phase C** decompose stage | GI-GS two-stage inverse solve ported onto the gsplat G-buffer + a pure-PyTorch deg-2 SH env light (replaces excluded nvdiffrast/nvdiffrec). Golden test recovers albedo **MAE 0.001** (<0.05). Tests 30→37. | design workflow (port plan + **license triage**) → implement → FULL high-tier panel (correctness/fail-closed/regression/security/flow) + one fix cycle (2 MAJOR + 4 MINOR) re-verified. |

## License posture (verified, on record)

`precompute/vendor/gigs/` is single-license MIT: `LICENSE` (verbatim) + `NOTICE` + `pbr_math.py`
(8 import-free functions extracted verbatim). The Inria `diff-gaussian-rasterization` fork,
`nvdiffrast`, `nvdiffrec` are NEVER copied — reference build stays in gitignored `scaffold/`.
A dedicated security-lens verifier confirmed **no restricted code in the tree or full git
history**, and that every reimplementation (SH env, PBR shade, depth→normal, losses) is
authored, not header-stripped. Triage + plan: `docs/validation-m2b-phaseC-portplan-2026-07-12.md`.

## OWNER CALL (surface before phase D / M2 completion)

decompose recovers a scene-global environment and emits `env_sh.json`; `export --from-decompose`
writes a coordinate-flipped env-SH sidecar. **But the M2a Godot runtime still uses a flat ambient
constant** — it does not yet consume the sidecar. Wiring the Godot `ambient_sh(N)` reader (using
the shared `precompute/core/sh_env.py` basis/A_l/flip constants so it matches exactly) is a
coordinated exporter+importer follow-up, handled "like a schema change" (logged in decisions.md).
No per-Gaussian schema change is involved (SCHEMA_VERSION stays 1). **Confirm the sidecar format +
that the Godot reader shares the sh_env constants before wiring it.**

## Why stopped before phase D

Phase D = run decompose on the REAL assets (`pxl_144634`/`pxl_131945`) and confirm the held-out
re-render is within a dB budget of train_base (the gate is built + default-ON, invariant #8).
This is (a) **expensive real-data validation** — the factory's rule is to SCHEDULE it as a
one-shot with a findings doc, not poll it in-loop; (b) **convergence-uncertain** on real foliage
(thin leaves are the known-hard inverse-rendering case per CLAUDE.md) → likely iterative param
tuning, a multi-run effort; (c) gated in spirit on the env-SH owner call above. The entire
buildable/testable M2 is shipped; phase D is the validation-on-real-data tail.

## Where it stands / next

- **Phase D** (`tasks/2026-07-11-m2-decompose.md`, remaining): schedule a real-asset decompose
  run on the 3090 (idle), record `metrics_decompose.json` re-render PSNR vs train_base, tune to
  meet the ≥ train_base − 1.5 dB budget, then `export --from-decompose` the real relightable asset.
  Expect thin-leaf messiness (the `trans` channel / M3 is the mitigation, not a phase-D bug).
- **DECISIONS:** D2 (foliage Gaussian budget — data-backed, owner picks) and D3 (M4 orientation,
  gated to M4) remain open. Neither blocks phase D.
- **Planner reconcile done at this wrap-up (post-disarm):** M2a's one-line GDGS diff folded into
  `docs/decisions.md` (invariant #6); M2b A/B/C decision entry; QUEUE groomed; memory updated.
- The JAX/M3 transmission task remains an EXTERNAL contributor lane (not factory work).

## Operational notes

- `commands.build` = `SMOKE_REQUIRE_ASSET=1 bash precompute/smoke.sh` runs before every commit;
  green throughout. `test_decompose.py`/`test_gbuffer_smoke.py` are CUDA-gated (skip on CPU).
- The 184 MB `godot/gs_assets/pxl_144634.relightply` and `scaffold/` are gitignored (local only).
- Implementer subagents must validate in the FOREGROUND (a background sweep once returned empty).
