# Dark-factory run #9 handoff — 2026-07-17

One task worked: the D6 grazing-normal-resolver. **Outcome: partial.** The resolver
infrastructure and the two gate-defect fixes shipped (v0.18.0), rigorously verified; but the
owner-approved D6 hybrid's real-data efficacy target (<5% sign opposition) is **REFUTED** — the
~30% floor is balance-invariant on real foliage. Hero assets are UNCHANGED (fail-closed). D6 is
reopened as **D7**.

## Shipped — v0.18.0 (tag v0.18.0)

| item | verification |
|---|---|
| **D6 hybrid sign resolver** (visibility-witness `d_peak` orientation + coarse-voxel sign field; replaces the k_cam nearest-camera vote as the default decompose sign pass) | high-tier panel on the first impl (correctness+fail-closed+regression+security+flow-verifier) → **2 MAJOR** found → fix cycle 1 → independent correctness **re-verify** by fault injection: MAJOR-1/MAJOR-2 CONFIRMED-FIXED |
| **Gate defect #1** — FATAL refusal now exits nonzero (`GATE_REFUSED_EXIT=3`) | fail-closed judge + flow-verifier (real OS exit 3); **proven in production** (both re-decompose runs refused, exit 3) |
| **Gate defect #2** — refused run no longer clobbers tracked `metrics_decompose.json` | byte-identical tracked file after refusal + refused-only file (gitignored); proven in production |

Suite 107 → **114**. Tree clean; nothing pushed (`allow_push:false`).

## The verification arc
- Original 5-lens panel (on the first implementation): fail-closed / regression / security / flow-verifier all **green**; **correctness found 2 MAJOR** — (1) cue (a) could lock a *confidently-wrong* sign a crowd of one-sided grazing views dragged the aggregate to, which the voxel field was then forbidden to fix; (2) the synthetic fixture was one-sided and could not exercise (1).
- **Fix cycle 1:** orientation now follows the MOST-face-on witness `d_peak` (not the count aggregate); confident only when the aggregate `ref` corroborates the witness (else handed to the voxel field). MAJOR-2's fixture was replaced with `_aggregate_trap_scene`, a genuine aggregate-vs-witness trap (aggregate → band 0% correct; witness → 98%).
- **Independent re-verify (fault injection):** injecting the full pre-fix bug reproduces the −X-confident failure and drops the new test to `a_res=0.53` (fails `>0.75`); revert → pass. MAJOR-1 & MAJOR-2 **CONFIRMED-FIXED**. One residual MINOR: on a pathological single-witness synthetic the voxel field is net-negative — deferred; the fail-closed gate protects.

## The refutation (the load-bearing result)
Re-decompose of pxl_144634 (2.4M splats, `--smooth-normals-iters 2`), run to a scratch out-root so
shipped artifacts were never at risk:

| config | 8-NN opposition | adaptive-domain | vis / voxel share |
|---|---|---|---|
| default balance | **30.03%** | **37.65%** | 88.5% / 11.5% |
| voxel-dominant (`--sign-vis-min-coherence 0.9`) | **29.13%** | **37.81%** | 69.3% / 30.6% |
| (pre-fix v0.16.0 camera-hemisphere, for reference) | 29.6% | 35.4% | — |

The ~30% floor is **balance-invariant**: shifting nearly 3× more work to the neighbor-consensus
voxel field barely moved it. Diagnosis: on real grazing foliage normals the front/back is genuinely
ambiguous, so visibility-witness orientations are high-confidence but *neighbor-inconsistent*, and the
voxel field (which only gets the low-confidence residual) can't override them. PSNR held (21.5 dB, fine).
Both runs correctly **REFUSED** (run 1 on the PSNR-budget gate — scratch lacked the train_base baseline,
correct fail-closed; run 2 on the sign gate 29.13% > 5%). pxl_131945 not re-run — D6 already measured it
at 30.99%/36.8%, and balance-invariance on 144634 is conclusive.

## Why ship at all (v0.16.0 precedent)
The gate defects are unambiguous bugfixes worth shipping regardless (a fail-open exit code is a safety
bug; #2 protects the shipped artifact↔metrics pairing — and it just did, twice). The resolver is
verified-correct, non-regressing, fail-closed substrate for whatever D7 picks; making it the default is
operationally inert (both it and the old vote sit at ~30% and both REFUSE, so no bad asset can ship).
This mirrors v0.16.0, which shipped the normal-sign infra + fail-closed gate with efficacy pending — that
"pending" is now answered: refuted.

## Operational notes
- **Concurrency near-miss (the run #8 hazard, recurred):** the fix-cycle implementer subagent's
  completion notification arrived very late; a file-stabilization heuristic fired during a compute pause
  and I began finishing the MINORs / a fault-inject by hand while the agent was still writing the same
  files. The final tree was verified CONSISTENT (no duplication; my edits and the agent's landed
  disjoint), but next run: do **not** hand-edit files a subagent is still writing — wait for its real
  completion notification, not a log/mtime proxy.
- Two real re-decompose runs (~7–8 min each) on the local 3090 (idle, verified) served as the app-smoke.
- The gate-defect fixes are the reusable win here — every future re-decompose is now safe against
  fail-open exits and metrics clobber.

## State
- HEAD on `main` at the v0.18.0 commit + tag (local; not pushed). Suite 114. Both heroes' built/mirrored
  assets **UNCHANGED**. Refused metrics preserved under the session scratchpad (not committed).
- OPEN walls: **D7** (next sign-resolution approach — the new top wall, gates M3), **D3** (M4 orientation,
  gated to M4). D6 marked implemented-but-refuted, superseded by D7.
- Queue after this run: no unblocked Ready task advances M3 until D7 is answered. Fillers remain:
  pixel5-variants (#4, better after a working sign approach), quality-pass slices 6/7, docs-guide.

## The 1–2 questions that unblock the highest-value track
1. **D7 — the D6 hybrid is refuted (~30% floor, balance-invariant). Which way?** Recommended order:
   (a) **accept the current look** (relight read good at the D5 eyeball; proceed to M3 with the caveat);
   (b) anisotropy/confidence-gate (shade only decisively-flat normals, isotropic elsewhere — no fake
   shadow rather than a forced sign); (c) global MST/graph-cut sign optimization (the thing the O(N)
   hybrid can't do); (d) sign-agnostic shading (`|dot(N,L)|` / two-lobe wrap) so opposition stops
   mattering. This is the M3 gate.
2. Anything to do with the shipped resolver default meanwhile? (It's inert + fail-closed; no action
   needed, but flagging since it changed the decompose default.)
