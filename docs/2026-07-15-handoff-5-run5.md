# Dark-factory run #5 handoff — normal-quality STEP 2 (shipped) + lighting-stability (WIP)

Prior runs: `docs/2026-07-12-handoff.md` (#1) · `-handoff-2-M2.md` (#2) · `-handoff-3-run3.md` (#3)
· `docs/2026-07-14-handoff-4-run4.md` (#4). This is #5 (owner "factory armed now" over the
2026-07-14-late-groomed queue: normal-quality STEP 2, then lighting-stability).

**Branch:** main · **WD:** /home/lukas/splatworld · **Factory:** disarmed at end · nothing pushed
(`allow_push:false`; owner pushes after review).

## Shipped this run (verified — never self-reviewed)

| version | item | verification |
|---|---|---|
| **v0.13.0** | **normal-quality STEP 2 — the D5 fix.** k-NN normal smoothing (`precompute/core/normals.py`) folded into `decompose` before its fail-closed held-out-PSNR gate (opt-in `--smooth-normals-iters`, default **0 = exact no-op**). | correctness + regression + flow-verifier panel **all clean**. Real re-decompose of pxl_144634: PSNR **21.572 dB (−0.11 dB, ≤1.5 ✓)**, shimmer **48.77 (−75.3%, ≤98.8 ✓)**, coherence 0.579→0.922 (over-smooth tripwire clear). Suite 78→88. Commit `578582d`, tag v0.13.0. |

## Decision-changing / owner-input surface

1. **Design fork resolved → decompose-side (was UNCERTAIN in the #4 handoff).** Once the held-out
   re-render PSNR on smoothed normals is mandatory, export-side isn't cheaper (you build+run a
   renderer either way) while decompose already owns a *trusted* one. New pixel5 variants decompose
   fresh → smoothed normals for free. Rationale in `docs/validation-normal-quality-step2-2026-07-15.md`.
2. **normal-quality rollout is DEFERRED (opt-in default-OFF).** The built/mirrored
   `pxl_144634.relightply` the viewer loads is UNCHANGED (still unsmoothed) — so the owner will still
   see sparkle in the viewer until the assets are re-shipped with smoothing ON. That re-ship
   (+ pxl_131945, + demo/gif regen) is **recurring-quality-pass slice 5**, now unblocked. The
   validated smoothed decompose for pxl_144634 is preserved at `.perf/normalsmooth/pxl_144634/`
   (gitignored) ready to promote — coherence 0.922, PSNR 21.572.

## lighting-stability — WIP, NOT shipped (Ready #2)

The implementer built `godot/relight/tools/render_matrix.gd` (803 lines) but hit heavy operational
friction (the harness auto-backgrounds "foreground" renders; it never got a clean full run) and a
mid-session usage-limit death. I took over, fixed 2 init bugs (`%.6g`→`%.6f`; deferred the
sphere-screen `unproject` to the first live frame), and drove the full 53-condition matrix to
completion. **Result: 6/10 checks PASS, 4 FAIL — all 4 are HARNESS-logic bugs, NOT relight-engine
findings** (so NO DECISIONS row). Committed as WIP for efficient resumption; details + the exact 4
fixes are in the file's WIP header comment.

- **PASS (engine core is stable):** azimuth-360 return **59.2 dB** (>45), elevation smoothness
  (max jump 0.036 < 0.12), ambient floor (p2 0.031 > 0.015 at flat ambient 0.5), relit luma bounds
  (0.159–0.433 in (0.01,0.98), over-drive excluded), min coverage (2340 > 1500), no NaN/Inf.
- **FAIL → the 4 harness fixes for the next run:**
  1. `sphere_consistency` n_px=0 on all 16 grid conds — the gray reference sphere is placed ~0.85·r
     ABOVE center, outside the fixed camera frame, so it never renders. Reposition it into view.
  2. `energy_linearity` ratio==1.0 on low pairs — the ambient term isn't nulled; run the energy sweep
     at ambient=0 + env-SH cleared (the 2→4 pair already reads 1.90).
  3. `raw_invariance` 14 dB — full-frame compare includes the engine-lit sphere; mask to foliage (cov).
  4. `trans_inertness` 45 dB (want 55) — readback/sort nondeterminism at 4× on a few pixels
     (means match to 1e-4); mask foliage + raise settle, or set a realistic threshold (~40 dB).

## Where it stands / queue after this run

- **Ready #1 normal-quality STEP 2 — SHIPPED v0.13.0.** Task file bannered.
- **Ready #2 lighting-stability — WIP** (`render_matrix.gd` committed; 4 harness fixes above; then
  re-run on DISPLAY=:0 + verify + banner). Non-blocking (M3 depends on #1, which is done).
- **Ready #3 pixel5-variants** — still ready (D2 decided, orientation confirmed); prefer after the
  normal-quality rollout (slice 5) so variants export with smoothed normals.
- **recurring-quality-pass slice 5** (asset re-ship with smoothed normals + demo/gif regen) — now
  unblocked by v0.13.0; the validated smoothed decompose is staged in `.perf/normalsmooth/`.

## The 1–2 questions that unblock the highest-value track

1. **Roll out the normal-quality fix to the viewer asset?** (promote `.perf/normalsmooth/pxl_144634`
   → built + re-export + re-mirror to gs_assets, then pxl_131945, then regen the demo). This is what
   actually removes the sparkle the owner saw. Default-OFF means it hasn't happened yet.
2. **Finish lighting-stability** (4 harness fixes) or re-prioritize it below pixel5-variants?

## Git state (safety-net)

```
$ git log -4 --oneline
<this wrap-up commit>  docs: run #5 handoff + lighting-stability WIP tool
578582d normal-quality: k-NN normal smoothing fix, decompose-side + PSNR-gated (v0.13.0, D5 step 2 of 2)
353d81d Handoff #5 (factory run4) + planner postscript: alignment eyeball PASSED, pixel5 ungated
2a6faae Orientation eyeball PASSED: ungate pixel5-variants to Ready #3; seed demo-regen slice; groom for run #5
```
Tags v0.2.0 … **v0.13.0**. Factory disarmed. Nothing pushed. Suite: 88 passed.

## Next action

Highest value = **roll out the normal-quality fix to the viewer asset** (removes the D5 sparkle the
owner reported) — slice 5, validated smoothed decompose already staged. Then finish lighting-stability
(4 harness fixes) and pixel5-variants → M4.
